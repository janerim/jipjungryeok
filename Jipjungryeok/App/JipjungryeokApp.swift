import SwiftUI

@main
struct JipjungryeokApp: App {

    var body: some Scene {
        WindowGroup {
            RootView()
        }
        // §10 — `.preferredColorScheme` 을 지정하지 않는다. 시스템 설정을 그대로 따른다.
    }
}
